kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: discourse
  namespace: discourse
  labels:
    app: discourse
spec:
  to:
    kind: Service
    name: web
    weight: 100
  port:
    targetPort: 3000
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Allow
